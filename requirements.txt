# t-Spanner Project — Python Dependencies
# Install: pip install -r requirements.txt

networkx>=3.0
matplotlib>=3.8
numpy>=1.26
scipy>=1.12
tqdm>=4.66

# Optional: for Streamlit dashboard
streamlit>=1.30
pyvis>=0.3

# Optional: for OpenStreetMap road network
# osmnx>=1.9
# Note: osmnx requires additional system dependencies (GDAL, etc.)
# If osmnx fails, the code will use a synthetic road network fallback
